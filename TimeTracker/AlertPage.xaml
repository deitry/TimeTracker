<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="using:TimeTracker"
             x:Class="TimeTracker.AlertPage"
>

    <ContentPage.BindingContext>
        <local:AlertViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Text="{Binding TrackerName}" />
            <Label Grid.Row="1" Text="{Binding TrackerMessage}" />
            <Button Grid.Row="2" Text="Ok" Clicked="Button_OnClicked" />
        </Grid>
    </ContentPage.Content>
</ContentPage>
